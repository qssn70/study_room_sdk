least_conn;
# Keep failed containers eligible for the next request so a rolling
# replacement can immediately retry the healthy sibling instance.
server api-1:3000 resolve max_fails=0;
server api-2:3000 resolve max_fails=0;
