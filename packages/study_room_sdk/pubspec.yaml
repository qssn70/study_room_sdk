name: study_room_sdk
description: Core Flutter SDK for integrating online study rooms.
version: 0.3.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  http: ^1.2.2
  socket_io_client: ^3.0.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
