name: study_room_ui
description: Optional Flutter widgets for the online study room SDK.
version: 0.3.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.10.5
  shared_preferences: ^2.3.3
  study_room_sdk:
    path: ../study_room_sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0

flutter:
  assets:
    - assets/audio/
