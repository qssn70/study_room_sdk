name: study_room_example
description: Example Flutter app integrating the study room SDK and UI package.
publish_to: none
version: 0.4.0-beta.1

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  study_room_sdk: ^0.4.0-beta.1
  study_room_ui: ^0.4.0-beta.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true

