# Study Room Demo

Open `index.html` in a browser to try a local, dependency-free online study room demo.

This demo is a static mock that shows how an app could compose the SDK concepts:

- room presence
- focus timer
- chat messages
- connection state
- session summary

It does not call the NestJS backend. Use `apps/example_flutter` for the Flutter package integration example.

